{
  "name": "js-dos-template",
  "version": "1.0.0",
  "description": "js-dos 6.22",
  "scripts": {
    "start": "http-server --host 0.0.0.0 --port 80",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
